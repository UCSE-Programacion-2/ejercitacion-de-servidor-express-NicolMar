---
title: 'Requisito: Parámetro de ruta en usuarios'
labels: auto-issue
---

El servidor debe manejar una petición `GET` a la ruta `/usuarios/:id`, donde `:id` es un parámetro dinámico en la URL, y devolver la respuesta correspondiente con el ID.
